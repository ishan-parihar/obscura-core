[project]
name = "obscura-core"
version = "0.1.0"
description = "Core library for Obscura browser integration with cookie management, daemon service, connection pooling, and plugin system"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
    "aiofiles>=24.0.0",
    "watchfiles>=0.24.0",
    "httpx>=0.27.0",
    "loguru>=0.7.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "browser-cookie3>=0.10.0",
    "cryptography>=42.0.0",
]

[project.urls]
Homepage = "https://github.com/ishan-parihar/obscura-core"
Repository = "https://github.com/ishan-parihar/obscura-core"
Issues = "https://github.com/ishan-parihar/obscura-core/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.6.0",
    "mypy>=1.0.0",
]

[project.scripts]
obscura-daemon = "obscura_core.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
